1. What is the difference between shutil.copy() and shutil.copytree()?
Ans-	shutil.copy() is used to copy a single file from the source to the destination. 
   	shutil.copytree()` is used to recursively copy an entire folder from the source to 		the destination.


2. What function is used to rename files?
Ans-	shutil.move()


3. What is the difference between the delete functions in the send2trash and shutil modules?
Ans-	send2trash functions will move a file or folder to the recycle bin
	shutil functions will permanently delete files and folders.


4. ZipFile objects have a close() method just like File objects’ close() method. What ZipFile method is equivalent to File objects’ open() method?
Ans-	The zipfile.ZipFile() function is equivalent to the open() function; the first argument is the filename, and the second argument is the mode to open the ZIP file in (read, write, or append).
